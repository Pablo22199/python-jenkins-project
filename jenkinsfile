pipeline {
    agent any

    stages {
        stage('Clone Repo') {
            steps {
                echo 'Cloning repository...'
                // Jenkins auto-clones, this is just to mark the stage
            }
        }

        stage('Install Dependencies') {
            

        stage('Run Python App') {
            steps {
                sh 'calculator.py'  // 
            }
        }
    }
}
